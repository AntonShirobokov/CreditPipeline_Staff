spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/CreditPipeline
    username: postgres
    password: postgres
  sql:
    init:
      mode: never

management:
  endpoints:
    web:
      exposure:
        include: beans
server:
  port: 8081


